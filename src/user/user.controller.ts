import { getUserById, getAllUsers } from './user.repository';
import { Request, Response } from 'express';

export async function getAll(req: Request, res: Response) {
    try {

    } catch (err){
        
    }
}